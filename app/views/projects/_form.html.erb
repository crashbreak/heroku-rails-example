<%= form_for(@project) do |f| %>
  <div class="field">
    <%= f.label "You can crete #{params[:available_project_count]} more projects" %>
  </div>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :employees_count %><br>
    <%= f.number_field :employees_count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
